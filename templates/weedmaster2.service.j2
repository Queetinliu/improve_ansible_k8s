[Unit]
Description=SeaweedFS master
After=network.target

[Service]
Type=simple
User=root
Group=root

ExecStart=/usr/local/bin/weed  -v={{ loglevel }} master -ip={{ masterip }} -mdir={{ weedmasterdir2 }} -port={{ weedmasterport2 }} -peers={{ weedmasterpeers2[:-1] }} -defaultReplication={{ weedreplication }}
WorkingDirectory=/opt
SyslogIdentifier=seaweedfs-master

[Install]
WantedBy=multi-user.target
